export declare class Pair<T, E> {
    first: T;
    second: E;
    /**
     * Creates new instance of a pair
     * @param first First value (default = null)
     * @param second Second value (default = null)
     */
    constructor(first?: T, second?: E);
    /**
    * Prints values into the console
    */
    Print(): void;
}
